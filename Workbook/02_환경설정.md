# 02. 환경 설정

## 1. Python 3.11+

```bash
# 설치 확인
python --version
```

## 2. Node.js 18+

```bash
node --version
npm --version
```

## 3. Kafka (KRaft 모드 — Zookeeper 없음)

> Kafka 4.x부터 KRaft 전용. Zookeeper 설정 필요 없음.

### 설치
1. [kafka.apache.org](https://kafka.apache.org/downloads) 에서 다운로드
2. `C:\kafka`에 압축 해제

### 초기 설정 (최초 1회)
```powershell
# Cluster ID 생성
C:\kafka\bin\windows\kafka-storage.bat random-uuid
# 출력된 UUID를 아래 명령에 사용

C:\kafka\bin\windows\kafka-storage.bat format `
  -t <위에서-나온-UUID> `
  -c C:\kafka\config\kraft\server.properties
```

### 실행
```powershell
C:\kafka\bin\windows\kafka-server-start.bat C:\kafka\config\kraft\server.properties
```

### 토픽 생성 (최초 1회)
```powershell
C:\kafka\bin\windows\kafka-topics.bat `
  --create --topic crawled-products `
  --bootstrap-server localhost:9092 `
  --partitions 1 --replication-factor 1
```

## 4. MongoDB (WSL2)

```bash
# WSL2 터미널에서
sudo systemctl start mongod

# 상태 확인
sudo systemctl status mongod
```

> MongoDB 설치가 안 된 경우: `sudo apt install mongodb` 또는 공식 문서 참고

## 5. Redis

```powershell
# Windows - redis-server.exe 실행
redis-server

# 또는 WSL2에서
sudo service redis-server start
```

## 6. MariaDB

```powershell
# Windows 서비스로 이미 설치된 경우
Get-Service -Name "MariaDB"
Start-Service -Name "MariaDB"
```

## 7. Python 패키지 설치

각 컴포넌트 디렉터리에서 설치:

```powershell
# Crawler
cd Crawler
pip install -r requirements.txt
playwright install chromium

# Kafka Consumer
cd Kafka
pip install -r requirements.txt

# Product gRPC 서버
cd Backend/product
pip install -r requirements.txt

# Gateway
cd Backend/gateway
pip install -r requirements.txt
```

## 8. Frontend

```powershell
cd Frontend
npm install
```

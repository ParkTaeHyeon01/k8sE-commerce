apiVersion: v1
kind: Secret
metadata:
  name: mongodb-kevin-password
  namespace: mongodb-ns
type: Opaque
stringData:
  password: "k8spass#"
---
apiVersion: mongodbcommunity.mongodb.com/v1
kind: MongoDBCommunity
metadata:
  name: mongodb
  namespace: mongodb-ns
spec:
  members: 3
  type: ReplicaSet
  version: "6.0.5"
  security:
    authentication:
      modes: ["SCRAM"]
  users:
    - name: kevin
      db: ecommerce
      passwordSecretRef:
        name: mongodb-kevin-password
      roles:
        - name: readWrite
          db: ecommerce
      scramCredentialsSecretName: kevin-scram
  statefulSet:
    spec:
      volumeClaimTemplates:
        - metadata:
            name: data-volume
          spec:
            accessModes: ["ReadWriteOnce"]
            storageClassName: longhorn
            resources:
              requests:
                storage: 10Gi

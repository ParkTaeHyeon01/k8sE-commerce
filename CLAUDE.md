# 쿠팡 인기/할인 상품 프로젝트

## 기본 지시
- 항상 한국어로 답변한다
- 코드 주석은 한국어로 작성한다
- 단순하고 필요한 코드만 작성한다 — 과도한 추상화 금지
- 발표용 MVP이므로 동작하는 핵심 흐름에 집중한다

## 모델
- 기본: Sonnet / 단순 작업: Haiku / Opus 사용 안 함

## Observability
- 로그는 Python `logging` 모듈 사용 (외부 라이브러리 최소화)
- 트레이스 ID를 요청마다 생성하여 크롤러 → Kafka → 백엔드까지 같은 ID로 추적
- 로그 포맷: `[trace_id] 컴포넌트 | 레벨 | 메시지` 형태로 통일
- FastAPI는 미들웨어에서 trace_id 자동 생성 및 헤더/로그에 포함
- 로그 레벨: DEBUG(개발) / INFO(흐름) / ERROR(실패) 3단계만 사용